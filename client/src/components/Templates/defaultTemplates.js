/**
 * User-specific templates module.
 * Default templates are empty so templates and saved codes are strictly user-specific.
 */
export const INITIAL_SEEDED_TEMPLATES = [];

export default INITIAL_SEEDED_TEMPLATES;
